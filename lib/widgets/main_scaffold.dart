// Bottom-nav composition currently lives inline in HomeScreen
// (lib/screens/home/home_screen.dart) since it owns the IndexedStack
// for Home / Orders / Profile. This file is kept as the place to extract
// a shared scaffold from if more top-level tabs (e.g. Favorites) are added.
